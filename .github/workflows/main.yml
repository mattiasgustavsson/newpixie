name: build

on: [push, pull_request]

jobs:
    windows:
        runs-on: windows-latest
        steps:
        - uses: actions/checkout@v1
        - name: msvc-x64
          run: |
            cd runtime
            cl ..\source\main.c
